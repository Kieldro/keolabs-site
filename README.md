# Keo Labs — landing page

Astro + Tailwind v4. Deploys to GitHub Pages at `keolabs.ai` via `.github/workflows/deploy.yml`.

## Dev

```bash
npm run dev      # local dev server
npm run build    # static build into dist/
npm run preview  # serve dist/
```
